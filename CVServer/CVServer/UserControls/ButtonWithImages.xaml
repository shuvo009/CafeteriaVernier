<UserControl
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
	mc:Ignorable="d"
	x:Class="Procesta.CvServer.UserControls.ButtonWithImages"
	x:Name="UserControl" HorizontalAlignment="Right" VerticalAlignment="Center"
    DataContext="{Binding RelativeSource={RelativeSource Self}}">

	<Grid x:Name="LayoutRoot" HorizontalAlignment="Center" VerticalAlignment="Center">
        <telerik:RadButton HorizontalAlignment="Left" VerticalAlignment="Top" Click="button_Click">
			<StackPanel Orientation="Horizontal">
                <Image x:Name="ButtonImage" Source="{Binding ButtonImages}" Margin="0,0,0,0"/>
                <TextBlock x:Name="ButtonContent" Text="{Binding Content}"  Margin="4,-2,6,0" FontFamily="{Binding FontFamily}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
			</StackPanel>
		</telerik:RadButton>
	</Grid>
</UserControl>